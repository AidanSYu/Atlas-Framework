# Atlas frontend environment — copy to .env.local on each machine
# (.env.local is gitignored and does NOT transfer via git)

# Base URL of the Atlas backend (run_server.py serves on :8000 by default)
NEXT_PUBLIC_API_URL=http://localhost:8000
